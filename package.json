{
	"name": "template-worker",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy index.js",
		"dev": "wrangler dev src/index.js",
		"test": "vitest"
	},
	"devDependencies": {
		"wrangler": "^3.0.0"
	}
}
